# Results

> **Status:** empty by design (Milestone 0). No evaluations have been run, so
> there are no numbers to report. This file is populated once the scoring and
> suite-runner milestones land (Milestones 8–9, 12).

## Why this is empty

Publishing placeholder or fabricated numbers would violate the project's
quality bar. Every result in this repository must be backed by a reproducible
run and an inspectable trace. Until the runtime, tracing, and scoring exist,
there is nothing honest to put here.

## Planned result tables

When evaluations run, this document will contain, per task and agent:

| task id | agent | pass/fail | runtime (s) | tool calls | commands | failure reason |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |

Regression comparisons (improved / regressed / unchanged / flaky) will be added
alongside, with links to the underlying traces.

## Reproducing results

See [`REPRODUCIBILITY.md`](REPRODUCIBILITY.md). All default runs are CPU-only
and require no paid API.

## Relevant files

- `results/` (raw / processed / reports — created when evals run)
- `docs/METHODOLOGY.md`, `docs/PLAN.md`
